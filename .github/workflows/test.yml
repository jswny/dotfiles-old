name: Test
on:
  push:
    branches-ignore:
      - master
      - develop

jobs:
  docker_build:
    name: Docker Build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: docker/build-push-action@v1
        with:
          repository: jswny/dotfiles
          push: false
